setCpm(60/4)

$pad: note("<[c3,g3,d4] [d3,a3,e4] [g2,d3,a3]>")
  .s("supersaw")
  .lpf(perlin.slow(12).range(400,1400))
  .attack(2)
  .release(3)
  .gain(0.25)
  .room(0.8)

$waves: s("~ ~ ~ ~")
  .s("white")
  .lpf(800)
  .gain(0.15)
  .pan(sine.range(0.2,0.8).slow(16))

$bell: note("<c5 ~ ~ ~, ~ g4 ~ ~>")
  .s("triangle")
  .attack(1)
  .release(2)
  .gain(0.3)
  .room(0.7)

$piano: note("<c4 ~ e4 ~ g4 ~ e4 ~>")
  .s("piano")
  .lpf(1200)
  .gain(0.3)
  .room(0.6)

$arp: (n("0 2 4 7 4 2")).scale("c4:major").slow(1)
  .s("piano")
  .gain(0.5)
  .lpf(500)
  .attack(0.2)
  .release(0.3)
  .pan(0.5)