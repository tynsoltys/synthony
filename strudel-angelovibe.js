setCpm(72/4)

$drums: s("bd ~ ~ ~, ~ sd ~ ~").bank("AlesisHR16")
  .gain(0.18)
  .room(0.15)
  .lpf(900)

$bass: n("0 ~ 5 ~ 4 ~ ~ ~").scale("D:lydian")
  .s("triangle")
  .lpf(280)
  .gain(0.22)
  .attack(0.4)
  .release(1.2)

$pad: chord("<Dmaj9 Bm7 Gmaj7 Asus2>")
  .s("supersaw")
  .gain(0.18)
  .attack(2)
  .release(3)
  .lpf(perlin.slow(12).range(800,2000))
  .pan(sine.range(0.3,0.7).slow(6))

$drones: s("sine")
  .note("<d3 f#3 a3 e3>".slow(6))
  .gain(0.07)
  .attack(3)
  .release(5)
  .lpf(perlin.slow(10).range(200,700))

$sinecluster: note("<d4 f#4 a4 b4 e5>")
  .s("sine")
  .gain(0.05)
  .attack(1.5)
  .release(3)
  .detune("<0.2 0.4 0.1>")
  .pan(perlin.slow(5).range(0,1))
  .lpf(perlin.slow(8).range(600,1800))

$gestures: s("crackle? ~ crackle? ~ ~")
  .gain(0.04)
  .attack(1)
  .release(2)
  .lpf(perlin.slow(4).range(800,3000))

$field: s("coffeeShopAmbience")
  .gain(0.25)
  .lpf(1500)
  .loopAt(12)

$wind: s("wind")
  .gain(0.08)
  .hpf(250)
  .loopAt(16)

$leadglow: n("<0 ~ 2 ~ 4 ~ 6 ~>").scale("D:lydian")
  .s("triangle")
  .gain(0.04)
  .attack(0.8)
  .release(1.8)
  .lpf(1600)
  .slow(4)
  .pan(sine.range(0.2,0.8).slow(9))

._pianoroll({ fold: 2 })
._scope({ height: 120, scale: 0.5 })