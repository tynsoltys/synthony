setCpm(78/4)

$drums: s("bd ~ ~ ~, ~ sd ~ ~").bank("AlesisHR16")
  .gain(0.25)
  .room(0.2)
  .lpf(900)

$bass: note("c2 ~ ab1 ~ g1 ~ ~ ~")
  .s("triangle")
  .lpf(300)
  .gain(0.25)

$field: s("coffeeShopAmbience")
  .gain(0.28)
  .lpf(1500)
  .loopAt(8)

$wind: s("wind")
  .gain(0.1)
  .hpf(300)
  .loopAt(16)

$gestures: s("crackle? ~ crackle? ~ ~")
  .gain(0.05)
  .attack(1)
  .release(2)
  .lpf(perlin.slow(4).range(800,3000))

$pulses: s("sine").note("c4 ~ ~ g3 ~ ~")
  .gain(0.06)
  .attack(0.5)
  .release(1)
  .slow(3)
  .pan(sine.range(0.3,0.7).slow(5))

$drones: s("sine")
  .note("<c3 eb3 g3>".slow(4))
  .gain(0.08)
  .attack(3)
  .release(4)
  .lpf(perlin.slow(10).range(200,900))

$sinecluster: note("<c5 eb5 g5 bb5>")
  .s("sine")
  .gain(0.05)
  .attack(2)
  .release(2.5)
  .lpf(perlin.slow(6).range(500,2000))
  .pan(perlin.slow(7).range(0,1))

$badalamenti: note("<c4 ~ eb4 ~ g3 ~ [eb4 g3] bb3 ~ [eb4 g3] bb3 ~>")
  .s("piano")
  .gain(0.35)
  .lpf(1600)
  .room(0.65)
  .attack(0.1)
  .release(1.2)
  .late(0.05)
  .swingBy(1/8,4)

._pianoroll({ fold: 2 })
._scope({ height: 120, scale: 0.5 })