setCpm(82/4)

$drums: s("bd ~ [~ bd] ~, ~ sd ~ sd").bank("RolandTR808")
  .gain(0.7)
  .room(0.3)
  .lpf(900)
  .distort(0.1)
  ._scope({ height: 120, scale: 0.5 })

$guitar: note("<a2 ~ ~ a3 ~ ~ ~ ~>")
  .s("sine")
  .superimpose(x => x.s("triangle").detune(0.4))
  .lpf(1500)
  .gain(0.3)
  .attack(0.2)
  .release(0.6)
  .room(0.6)
  ._pianoroll({ fold: 1 })

$pad: note("<[c3,e3,a3] [b2,e3,g3] [a2,d3,f#3]>")
  .s("supersaw")
  .detune(0.6)
  .lpf(perlin.slow(12).range(400, 1800))
  .gain(0.22)
  .attack(0.4)
  .release(1)
  .room(0.8)

$bass: note("a1 ~ a1 ~ g1 ~ e1 ~")
  .s("sawtooth")
  .lpf(300)
  .gain(0.45)
  .distort(0.2)
  .attack(0.05)
  .release(0.2)




  /// v2 with exhoes and crackles

  setCpm(82/4)

$drums: s("bd ~ [~ bd] ~, ~ sd ~ sd").bank("RolandTR808")
  .gain(0.7)
  .room(0.4)
  .lpf(900)
  .distort(0.15)
  .delay(0.25)
  .delaytime(1/3)
  .delayfeedback(0.35)
  ._scope({ height: 120, scale: 0.5 })

$guitar: note("<a2 ~ ~ a3 ~ ~ ~ ~>")
  .s("sine")
  .superimpose(x => x.s("triangle").detune(0.6))
  .superimpose(x => x.distort(0.4))
  .lpf(perlin.slow(6).range(800, 2200))
  .gain(0.32)
  .attack(0.25)
  .release(0.9)
  .delay(0.5)
  .delaytime(3/8)
  .delayfeedback(0.45)
  .room(0.7)
  ._pianoroll({ fold: 1 })

$pad: note("<[c3,e3,a3] [b2,e3,g3] [a2,d3,f#3]>")
  .s("supersaw")
  .detune(0.8)
  .lpf(perlin.slow(8).range(400, 1600))
  .gain(0.24)
  .attack(0.5)
  .release(1.2)
  .room(0.85)

$noise: s("crackle*16").gain(0.15).lpf(2000).distort(0.3)

$bass: note("a1 ~ a1 ~ g1 ~ e1 ~")
  .s("sawtooth")
  .lpf(280)
  .gain(0.46)
  .distort(0.25)
  .attack(0.07)
  .release(0.25)
  .delay(0.2)
  .delaytime(1/4)
  .delayfeedback(0.3)